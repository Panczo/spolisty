<!-- CALL TO ACTION Center -->
%section{:id => "cta2"}
  .container
    .row
      .col-md-12
        .text-center
          %h2 
            = t('main_site.cta.title')
          = link_to t('main_site.cta.button'), user_omniauth_authorize_path(:spotify), class: "btn btn-primary btn-cta"